# 杀数字项目专属规则

本文件规定本项目的当前规则。可用的上级 `../AGENTS.md` 仍优先；规则不一致时不得通过扩大栏目、位置、期数或缓存范围让测试通过。

## 修改与 GitHub 同步

仅修改用户授权范围。保留无关改动，不恢复 disabled 目标，不自动学习或写回业务规则。完成修改后先做离线测试与提交范围检查，再向已确认的 origin 推送；不得推送凭据、环境变量、缓存、锁、调试页和运行输出。不得强制覆盖他人提交。

## 数据和身份

配置是 `targets.json`。号码是01至49，顺序必须保持；数量严格等于该目标的count。非法号码使整组失效，禁止删坏号、补数、跨组拼数。只有JSON布尔值 `allow_duplicate_numbers: true` 才允许同组重复。

目标身份必须同时考虑URL、栏目锚点、文章身份、解析器及专属来源。不同作者可以共用列表URL，禁止仅按URL合并；使用 `domain.periods.target_identity`。显示名称不得擅自截断、删除末尾字符或模糊匹配。`disabled: true` 不抓、不输出、不入缓存、不判重。`forbidden.json` 仍是历史记录文件，不代替 disabled，也不能由空records推断允许恢复目标。

## 解析窗口与来源

通用默认窗口5，显式issue_position_window必须生效；醉八仙top_7、凤舞九天的固定窗口是3。先确认同一栏目、号码格式/范围/数量和重复规则，再按有效数据行取top前N或bottom后N；无效行、无关帖子不占窗口。每行可有多个号码组，必须保留窗口内全部合法组检查同期冲突，禁止第二次按号码组截断。

开始锚点缺失必须失败；配置结束锚点但缺失也必须失败。只有专属解析器已明确规定的文档边界可替代文本边界，例如师太的下一文档/严格全文结尾。不可或缺只取由标题与方向确定的主周期，不从旧周期补目标期。

每个SourceDocument独立解析，不把不同来源/帖子混成正文。证据必须对应原文指纹、栏目范围、期号、顺序和有效行位置。不得因为其他位置有相同号码就退回整页。用户API独立帖子仅在user_id、profile API来源和作者身份均明确匹配时，可使用source_identity代替正文作者锚点。

专属解析失败不得回退通用解析。专属可用期数发现复用该专属解析器。链式采集先唯一锁定指定期详情，链接按其来源文档URL解析；跨文档冲突不能被优先级掩盖。

## 单期、多期和输出

单期：`run_crawler_prompt.py` 或 `crawler.py --issues N`，仅接受一个期数。多期：`run_crawler_multi_prompt.py` 逐期调用单期并始终传 `--no-cache-update`。两者不得改变业务窗口。

默认输出目录保持为当前代码使用的：
`C:\Users\Administrator\Desktop\每天工具\爬虫合集\大围杀号生肖数据统一归纳`
可显式设置 `SHUZI_RESULTS_DIR` 覆盖。不得未经核对改为历史文档提到的“数据系列”目录。启动时打印实际目录；成功、失败、报告和运行清单写入同一目录。

单期TXT行仍为 `号码整串 站点名称`，文件名携带期数。手动指定期数不生成.bak。每次生成唯一run_id；输出定稿后写 `N期-杀数字-运行.json`，包含输出哈希和成功/失败身份。多期汇总仅信任本轮run_id且文件哈希一致的清单，不根据旧TXT存在与否判成功。全部访问被本地权限阻止时可保留旧TXT，但旧结果不能算本轮成功。

重抓入口 `retry_failed.py` 只处理指定失败TXT中的精确目标。结果必须通过正式校验；保留BOM、CRLF和未处理记录；成功TXT追加必须补足缺失末尾换行。失败TXT被外部修改时停止覆盖。重抓不读取/修改缓存，也不刷新已失效的运行清单。

## 缓存版本2

实时采集与判定不读取号码缓存。TXT定稿后才能更新 `recent_10_cache.json`；缓存异常不撤销TXT。保留现有“成功率严格大于85%才更新缓存”的策略。未更新/未完成时 `.cache-state.json` 必须阻止旧缓存被当成本轮完整状态；多期不修改缓存或该状态文件。

版本2按稳定目标身份分别维护近10期观察值，成功/失败同站同期互斥。新成功移除旧失败，新失败移除旧成功；本轮同时出现两种状态则拒绝覆盖。配置签名变化使旧范围证据失效。不能用全站最大期裁剪其他站点。

周期必须显式来自配置 `cycle_id`、参数 `--cycle-id` 或环境变量 `SHUZI_CYCLE_ID`，不得按系统年份或期号下降猜周期。跨周期连续性还需要上一周期的明确期数上限（`cycle_lengths` 或 `--previous-cycle-length`）。版本1记录不自动补周期：可保留观察，但不能生成正式无重复结论。新缓存也不能凭 generated_at 证明站点实际发布进度。

## 正式判重

正式“检测重复”BAT默认只读已有v2缓存（`--from-cache`），避免每次操作都重新抓取全部站点。网站历史重建必须显式使用 `--rebuild-history --cycle-id ...`；跨周期还必须提供上一周期真实长度。当前期位置窗口和历史深度是两个契约：先按 `issue_position_window` 验证当前边缘，再在同一来源内扩展近10期，禁止通过扩大当前窗口凑历史。每个活跃目标必须有配置匹配、周期明确、连续近10期的数据，号码合法、原序一致，无相关失败/冲突。空缓存、缺目标、缺期、周期不明、没有共同期号、未知来源、上次缓存未同步时只能“检测未完成”。8/9期历史特例不降低正式门槛。

只有相同期号与周期且号码原始顺序完全一致才累计连续重复；3至5期疑似、6期以上拒收。按退出码区分：0完整且未发现重复、4检测未完成、5疑似、6拒收、2参数/运行错误。检测未完成报告不得输出“不重复”“没有发现重复”“检测通过”。

从文件读取时，期数来自严格的 `N期-杀数字-成功.txt`；周期来自同次运行清单，或来自 `2026周期-N期-杀数字-成功.txt` 这种明确文件名。如行内另有期号必须一致，无法解析的非空行必须报告。一个全局 `--cycle-id` 只能标记不跨周期且连续的无周期文件集，不能把365期和1期强行归入同一周期。`--issues/--latest` 的实时全局范围只作诊断，不能据此证明各站点自己的最新期。自动历史探测无法处理链式列表或资料不足10期时必须明确未完成，不扩窗、不借旧期凑数。

## 采集边界与资源

HTTP优先；默认验证TLS证书；单站例外必须明确 `insecure_tls: true` 并提示来源可信度下降。默认同源，跨域资源必须在 `allowed_resource_hosts` 明列。只允许http/https，拒绝内嵌凭据、本地/私有IP和DNS指向私有地址；重定向重新校验，不接受HTTPS降级。这不是抵御恶意DNS重绑定的完整网络沙箱，部署仍应使用网络隔离。

HTTP只对暂时失败重试，最多2次请求；正式外层默认1次补抓。不使用多层curl重试。限制响应体和gzip展开大小。API非404异常不得转其他来源；API已存在正文但缺目标期，按解析失败处理。多个 `strdecode`/Base64 结果必须作为独立来源片段解析，禁止用换行或空格拼接后制造候选。只有“该独立文档确实没有候选”可以继续看下一来源；已出现锚点、身份、结束边界或专属结构错误时必须停止。

浏览器仅在需要渲染时升级，默认2个owner线程，可用 SHUZI_BROWSER_CONCURRENCY 显式设为1至4；各线程独占自己的同步Playwright/Chromium实例并复用；每次使用新context/page。禁止跨线程共享同步Playwright对象。指定期等待目标期；历史模式等待合法候选出现并等待DOM稳定，不使用networkidle加固定sleep。Future、队列及关闭均有外层期限，卡死worker退休并替换，整个池关闭只使用一个总期限。

## 验证

独立站点验证 `validate_failed_sites.py --name 名称 --issues 指定期` 只验证显式列表，不写正式文件。负向期数也需显式提供，不得把“目标期+不存在期”全部返回失败误当作正例通过。实验配置/解析只在隔离区域，不自动迁移正式配置。

离线测试：`python -m pip install -r requirements-test.txt`、`python -m pytest -q`。测试必须隔离输出目录并使用假传输，不能真实抓全站。可选真实Chromium测试只访问本地测试HTTP服务，不访问生产站。历史golden中的号码和顺序必须保持；历史源码哈希只是当时捕获信息，不能拿它禁止所有后续修复。GitHub CI覆盖Linux/Windows和指定Python版本；是否通过以当前提交的实际运行记录为准。
