# 2026-09-09 代码审查修复说明

审查基线：`f4797270cd289d22541ce3b71011a6a9d1fcc85f`。修复没有改动targets.json、forbidden.json、启用/停用名单或原始golden号码；没有运行全站生产抓取，也没有迁移用户电脑上的文件。

## 已修复的主要问题

- 按配置执行窗口；先验证行，再取top/bottom。非法号码使整组无效，不再删坏号凑数量；同一行第四组冲突也不会被截掉。
- 结束锚点缺失失败，来源证据不再退回整页；用户独立帖子用明确API身份取代文本作者锚点。保留数字规范化后的证据，校验指纹/方向/窗口/位置。
- 不可或缺不会从旧周期补抓；详情链接以其实际来源文档为基准，相互冲突的链接不能被优先级隐藏。
- 缓存v2按栏目身份分组，同一列表URL下的不同作者不会合并。成功/失败互相替换，配置变化使旧证据失效，不使用全站最大期裁剪其他目标。
- 缓存/实时/文件判重共用完整性门槛，不足连续十期、失败、周期不明或无共同期号都返回“检测未完成”，不会返回0或给出无重复结论。
- 文件判重兼容正式“号码 站点名”格式，从文件名取期数；拒绝行内期号冲突。单期入口拒绝混输多期。
- 多期结果通过本轮run_id和输出哈希认证，旧成功TXT不能让本轮失败站点从报告中消失。重抓追加补齐末尾换行，校验失败不删失败记录。
- 输出目录可配置，默认保持现用目录。成功、失败、报告、运行清单统一放在该目录。
- 默认TLS验证、显式跨域资源白名单、私有地址检查、重定向检查、响应体大小限制及有限重试。真实浏览器由两个独占线程持有并复用，避免同步API跨线程共享。
- 严格JSON布尔值和期数输入；批处理不再吞掉KeyboardInterrupt。测试运行与生产目录隔离。

## 更新后的使用方式

日常指定一期的方式不变：

```bat
python crawler.py --issues 251 --workers 16
```

需要生成可用于正式判重的周期记录时，明确设置周期标识。例如资料确实属于2026周期：

```bat
set SHUZI_CYCLE_ID=2026
python run_crawler_prompt.py
```

这个环境变量是事实声明，不是从时间推断。未设置时，实时抓取仍可正常输出TXT，但无周期缓存观察值不能用于正式无重复结论。

跨周期时，只有在确认上一周期确为365期的情况下使用：

```bat
python crawler.py --issues 1 --cycle-id 2027 --previous-cycle-length 365
```

不要把这个示例的365当成所有周期的固定长度。该上限会记录在缓存中；配置也可提供 `cycle_lengths`。

多期仍通过 `run_crawler_multi_prompt.py` 或多期BAT逐期运行，始终不更新缓存。

正式判重BAT现在读缓存，相当于：

```bat
python check_duplicates.py --from-cache --recent 10
```

退出码：0=完整无重复；4=检测未完成；5=疑似；6=拒收；2=参数/运行错误。成功率不超过85%的单期运行保留原缓存，但会把其状态标为未同步，阻止下一次判重沿用旧完整结论。

旧版v1缓存不会被偷偷标记为已验证的v2十期基线。新实时观察逐步替换同目标记录；需要十期完整、周期明确的记录才能获得正式结论。无法证明的周期边界和缺期不会自动补齐。历史回看不能为了凑缓存扩大业务窗口。

默认目录仍是：

```text
C:\Users\Administrator\Desktop\每天工具\爬虫合集\大围杀号生肖数据统一归纳
```

实际目录不同可在同一个CMD窗口设置 `SHUZI_RESULTS_DIR` 后启动；不要同时使用两个目录中的旧TXT。

## 验证方式与边界

```bat
python -m pip install -r requirements-test.txt
python -m compileall -q crawler.py check_duplicates.py retry_failed.py run_crawler_multi_prompt.py kill_numbers
python -m pytest -q
```

GitHub工作流检查Windows、Linux与Python3.11/3.13。可选真实Chromium测试需要安装Playwright和浏览器，并设置 `RUN_BROWSER_SMOKE=1`；它只访问隔离的本地HTTP测试服务。

单元测试不能证明146个启用来源的当前网页均可访问。TLS默认收紧、跨域资源默认收紧后，有问题的网站会明确失败；确需例外应只改已经人工核对的单个目标，不要全局关闭校验。来源无法确认、链式历史发现不支持或不足十期时仍应报告未完成，这是保护准确性的行为，不是以旧数据兜底。

新增的IP/DNS检查不是完整防DNS重绑定沙箱；对不可信站点部署时仍建议网络隔离。此次没有把生产爬虫当成攻击测试器，也没有对外部站点进行压力测试。
